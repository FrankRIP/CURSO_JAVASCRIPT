// Ciclo While

// tambien consta de 3 partes como un for por no en el mismo orden
//y se ejecuta siempre que una condicion sea verdadera

let i = 0; // Inicializamos en cero

while(i < 10 ){ //Esta en la condicion
    console.log(`Numero ${i}`)
    i ++ // este es el incremento
}